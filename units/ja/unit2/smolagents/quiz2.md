

# 小テスト（採点なし） [[quiz2]]

*Code Agent*、*Tool Calling Agent*、*Tools* セクションの理解度を確認しましょう。このクイズは任意であり、採点されません。

---

### Q1: smolagents において、`@tool` デコレータでツールを作成する方法と `Tool` のサブクラスを作成する方法の主な違いは何ですか？

これら2つのツール定義アプローチの違いを最もよく説明している選択肢はどれですか？

- **A)** <code>@tool</code> デコレータは検索ベースのツールに必須であり、<code>Tool</code> のサブクラスはテキスト生成タスク専用である
  - 💡 どちらのアプローチも、検索ベースやテキスト生成ツールなど、あらゆる種類のツールに使用できます。
- **B)** <code>@tool</code> デコレータはシンプルな関数ベースのツールに推奨され、<code>Tool</code> のサブクラスは複雑な機能やカスタムメタデータに対してより柔軟性を提供する ✅
  - 💡 正解です。デコレータによるアプローチはよりシンプルですが、サブクラス化によりカスタマイズされた動作が可能になります。
- **C)** <code>@tool</code> はマルチエージェントシステムでのみ使用でき、<code>Tool</code> サブクラスの作成はシングルエージェントのシナリオ用である
  - 💡 すべてのエージェント（シングルまたはマルチ）は、ツールを定義するためにどちらのアプローチも使用できます。そのような制限はありません。
- **D)** 関数を <code>@tool</code> でデコレートすると docstring が不要になるが、サブクラスには docstring を含めてはならない
  - 💡 どちらの方法でも明確な docstring が有益です。デコレータが docstring を置き換えることはなく、サブクラスでも docstring を持つことができます。


---

### Q2: CodeAgent は ReAct（Reason + Act）アプローチを使用してマルチステップタスクをどのように処理しますか？

CodeAgent がタスクを解決するために一連のステップを実行する方法を正しく説明している選択肢はどれですか？

- **A)** 各ステップをマルチエージェントシステム内の異なるエージェントに渡し、結果を統合する
  - 💡 マルチエージェントシステムはタスクを分散できますが、CodeAgent 自体は ReAct を使用して複数のステップを単独で処理できます。
- **B)** すべてのアクションを JSON に格納して簡単にパースできるようにしてから、一括で実行する
  - 💡 この動作は ToolCallingAgent の JSON ベースのアプローチに該当し、CodeAgent のものではありません。
- **C)** 内部の思考を書き出し、Python コードを生成し、コードを実行し、結果をログに記録するというサイクルを、最終的な回答に到達するまで繰り返す ✅
  - 💡 正解です。これは CodeAgent が使用する ReAct パターンを説明しており、反復的な推論とコード実行を含みます。
- **D)** 次のステップに進む前に、ビジョンモジュールに依存してコード出力を検証する
  - 💡 ビジョン機能は smolagents でサポートされていますが、CodeAgent や ReAct アプローチのデフォルト要件ではありません。


---

### Q3: Hugging Face Hub でツールを共有する主な利点は次のうちどれですか？

開発者がカスタムツールをアップロードして共有する最も適切な理由を選んでください。

- **A)** ツールが自動的に MultiStepAgent と統合され、検索拡張生成（RAG）が実現される
  - 💡 ツールを共有しても、検索やマルチステップのロジックが自動的にセットアップされるわけではありません。単にツールを利用可能にするだけです。
- **B)** 他の人があなたのツールを発見し、再利用し、追加のセットアップなしで自分の smolagents に統合できるようになる ✅
  - 💡 正解です。Hub で共有することで、誰でも（自分自身を含めて）ツールをダウンロードしてすぐに再利用できるようになります。
- **C)** CodeAgent のみがツールを呼び出せるようになり、ToolCallingAgent は呼び出せなくなる
  - 💡 CodeAgent と ToolCallingAgent の両方が共有ツールを呼び出すことができます。エージェントの種類による制限はありません。
- **D)** ツールが画像処理用の完全なビジョン対応機能に変換される
  - 💡 ツールの共有によって、ツールの機能が変更されたり、ビジョン機能が自動的に追加されたりすることはありません。


---

### Q4: ToolCallingAgent はアクションの実行方法において CodeAgent と異なります。正しい説明はどれですか？

ToolCallingAgent の動作を正確に説明している選択肢を選んでください。

- **A)** ToolCallingAgent はマルチエージェントシステムとのみ互換性があり、CodeAgent は単独で実行できる
  - 💡 どちらのエージェントも、単独で使用することも、マルチエージェントシステムの一部として使用することもできます。
- **B)** ToolCallingAgent はすべての推論を別の検索エージェントに委譲し、最終的な回答を返す
  - 💡 ToolCallingAgent は依然としてメインの LLM を推論に使用しており、検索エージェントのみに依存しているわけではありません。
- **C)** ToolCallingAgent はツール呼び出しと引数を指定する JSON 命令を出力し、それがパースされて実行される ✅
  - 💡 正解です。ToolCallingAgent はツール呼び出しを定義するために JSON アプローチを使用します。
- **D)** ToolCallingAgent はシングルステップタスク専用であり、1つのツールを呼び出した後に自動的に停止する
  - 💡 ToolCallingAgent は CodeAgent と同様に、必要に応じて複数のステップを実行できます。


---

### Q5: smolagents のデフォルトツールボックスには何が含まれており、なぜそれを使うのですか？

smolagents のデフォルトツールボックスの目的と内容を最もよく表している選択肢はどれですか？

- **A)** DuckDuckGo 検索、PythonInterpreterTool、最終回答ツールなど、よく使われるツールのセットが提供されており、素早いプロトタイピングに役立つ ✅
  - 💡 正解です。デフォルトツールボックスには、エージェント構築時に簡単に統合できるこれらの既製ツールが含まれています。
- **B)** デフォルトでは画像分類や OCR などのビジョンベースのタスクのみをサポートしている
  - 💡 smolagents はビジョンベースの機能を統合できますが、デフォルトツールボックスはビジョン専用ではありません。
- **C)** マルチエージェントシステム専用であり、単一の CodeAgent とは互換性がない
  - 💡 デフォルトツールボックスは、シングルでもマルチエージェントのセットアップでも、あらゆるエージェントタイプで使用できます。
- **D)** ベクトルストアからの大規模な質問応答のための高度な検索ベース機能が追加される
  - 💡 検索ツールを構築することは可能ですが、デフォルトツールボックスは高度な RAG 機能を自動的に提供するものではありません。


---

このクイズを完了おめでとうございます！🎉 難しい問題があった場合は、*Code Agent*、*Tool Calling Agent*、*Tools* セクションを再度確認して理解を深めましょう。全問正解であれば、堅牢な smolagents アプリケーションを構築するための準備は万全です！

---

<!-- nav -->

[⬅️ 前へ: ビジョン Agent](vision_agents.md) | [📚 目次](../../README.md) | [次へ: smolagents: テスト ➡️](final_quiz.md)
